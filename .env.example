# Create a fine-grained token with access only to required repositories.
GITHUB_PERSONAL_ACCESS_TOKEN=

# Personal access token for gitlab.dbodm.com (native MCP endpoint returns 404 there,
# so this instance is used with the @zereight/mcp-gitlab PAT-based server instead of OAuth).
GITLAB_PERSONAL_ACCESS_TOKEN=
